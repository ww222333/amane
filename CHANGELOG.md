# Changelog

## v1.0.8

### 修复

- **内容路由刮削站点下拉为空**: `augment_config_schema` 将已发现的影片源 ID 写入 `ContentRouteEntry.sites` 枚举 (适配 `{sites, prefixes}` 路由结构)

## v1.0.7

### 新功能

- **并入上游 v0.16.1**: 播放源插件、Android 客户端、LLM API 协议选择、桌面环境变量配置, 以及 0.13–0.16.1 修复与增强
- 保留本仓库独有能力: 未知内容路由、内容路由自定义前缀、整理失败目录、整理空源回收 (见 `docs/dev/fork-delta.md`)

## v1.0.6

### 新功能

- **未知内容路由**: 解析未识别的番号归入 `unknown`, 默认空站点链, 可在设置中配置
- **整理失败目录**: 媒体库可手填失败目录; 整理时可将无 Metadata 的正片整夹移入, 并可排除该目录
- **整理空源回收**: 可选在移动成功后将已无视频的源目录整夹移入 `.amane_trash`

## v0.16.1

### 🐛 修复

- **监听 `0.0.0.0` 或 IPv6 时菜单栏/托盘无法连接** (#215)
- **服务启动失败触发反复重启** (#215), 改为退避重试并在菜单显示失败原因

## v0.16.0

### ✨ 新功能

- **优化响应式设计** (#206)
- **播放器触屏优化** (#206)
- **Android 客户端** (#206), 独立发版
- **桌面版环境变量配置文件** (#204), 方便修改环境变量

### 🐛 修复

- **旧版系统 WebView 兼容性问题** (#206)
- **媒体库位于 UNC 路径时整理报路径逃逸** (#211), 路径模板的 UNC 前缀与盘符不再被折叠
- **javbus 封面缺图** (#201), 图片下载补充同源 Referer; 上游占位图不再判定为获取成功
- **Agent 工具调用失效** (#207), 许多第三方 API 不支持动态工具加载, 改为全量声明

### ⚡ 性能

- **Agent 工具调用优化** (#207), 压缩工具描述和返回值

## v0.15.0

### ✨ 新功能

- **优化移动端布局** (#120 / #191)
- **播放源顺序可调整** (#187)

### 🐛 修复

- **Faleno / DAHLIA 刮削失败** (#189 / #193)
- **其他修复**

## v0.14.0

### ✨ 新功能

- **播放器增强** (#177), 双击全屏; 长按方向键快进; 音量调节; 右键菜单复制时间戳
- **评论系统增强** (#181 / #185), 支持编辑, UI 重做, 链接渲染
- **插件页重做** (#184)

### 🐛 修复

- **桌面版标准库缺失** (#178), 打包完整标准库
- **其他前端缺陷修复**

### ⚡ 性能

- **首屏 JS 由 1.83 MiB 降至 877 KiB** (#179), 前端路由按路由拆分

## v0.13.0

### ✨ 新功能

- **定时任务展示 payload** (#151)
- **Feed 已读/未读及关键词过滤** (#152)
- **播放源插件系统** (#155)
- **LLM 翻译自定义提示词** (#173), 改动后旧译文缓存自动失效
- **LLM 翻译支持 3 种 API 协议** (#175), 与 Agent 对齐

### 🐛 修复

- **可空字段导致 DMM 刮削失败** (#174), 并区分解析失败与无可用元数据
- **NFO 默认模板与视频文件名一致** (#172)
- **多行文本框改用 Textarea** (#176)
- **统一按钮悬浮说明为 Tooltip**
- **英文复数 i18n**

## v0.12.0

### ✨ 新功能

- **补刮可分别选择影片与演员** (#149)
- **新增回收任务** (#144), 整理只处理范围内的媒体文件
- **mosaic 支持有码** (#143), 新增 `{content_type}` 与 `{name|=缺省}`

### 🐛 修复

- **JavDB 演员性别解析** (#150)

## v0.11.0

### ✨ 新功能

- **支持使用 CloudDrive2 webhook 监听文件变更** (#131), 性能优于轮询挂载目录
- **新增模板占位符 `{prefix}` / `{suffix}`** (#129 / #134) 表示番号前/后缀
- **允许配置 Agent 输出 token 上限** (#126)

### 🐛 修复

- **SOCKS 代理依赖缺失** (#135)
- **文件名 CRACKED 识别** (#133)
- **目录删除/移出后清理数据库条目** (#132)

## v0.10.0

### ✨ 新功能

- **优化影片详情页用户标签展示**, 独立分栏; 多选增减菜单

### 🐛 修复

- **将 `-U` / `-UC` 解析为破解而非无码** (#119)
- **定时任务设定及展示遵循本地时间** (#116)
- **字典表单修复**, 滚轮不再切换 tab; 增减空键不再卡住保存条

## v0.9.0

### ✨ 新功能

- **字段黑名单** (#111), 可按字段排除站点
- **详情页标签及对比来源面板中展示演员性别** (#109)

### 🐛 修复

- **跨库整理 UNIQUE 冲突** (#114), 整理后的路径离开本库时删除索引
- **NFC / NFD 路径重复登记** (#112), 同一文件不再写入两行
- **聚合类字段顺序** (#110), 海报、封面、剧照与评分按字段优先级排列

## v0.8.0

### ✨ 新功能

- **女演员模板占位符** (#107), 新增 `{actress}` / `{actresses}`
- **刮削时记录演员性别** (#106), 创建演员条目时写入性别

### 🐛 修复

- **超长片名与演员导致整理路径过长** (#104), 截断并追加省略号
- **字幕与视频配对** (#105), 允许在平铺目录下解析字幕
- **Windows 下找不到超分工具可执行文件** (#101 / #98)

## v0.7.2

### 🐛 修复

- **桌面端服务无法启动**

## v0.7.1

### 🐛 修复

- **站点配置无法切换使用代理** (#90 / #96)
- **附属资源模板展开后文字不可见** (#90 / #96)

## v0.7.0

### ✨ 新功能

- **允许刮削时指定番号** (#85), 覆盖自动识别结果
- **STRM 内容模板** (#87 / #42), 可自定义 `.strm` 正文; 新增占位符 `{video_relpath}`
- **优化媒体库配置表单**, 路径模板语法着色; 按链接模板显示 STRM 字段
- **新增 `{video_name}` / `{link_name}` 占位符** (#63), 可直接引用整理后文件名

### 🐛 修复

- **软链接模式二次整理冲突** (#67 / #58), `{video_dir}` 改为路径字面量, 不再解析软链接
- **识别 Txx 与六位无码番号** (#82)
- **设置页含点号的自定义键无法保存** (#83), 如网络速率限制
- **并发加载图片时写库冲突** (#84)
- **清除已完成任务时残留失败子任务**
- **日志页不足一屏时从顶部排布**

## v0.6.2

### 🐛 修复

- **AVSOX 刮削** (#55)
- **日志页自动滚动**, 结构化字段着色增强可读性
- **整理任务报告进度**, 仅遍历一次目录

## v0.6.1

### 🐛 修复

- **扫描网络盘导致应用阻塞**, 扫描、目录浏览、路径校验和整理时的磁盘操作不再阻塞其它请求

## v0.6.0

### ✨ 新功能

- **整理后创建软链接 / strm** (#7 / #36), 整理时可在库外创建 strm 或软链接, 从而实现影片与元数据分离, 可用于网络盘挂载场景
- **按文件大小过滤视频** (#38 / #39), 小于阈值的视频不入库, 整理时移入 `.amane_trash`
- **增强路径模板** (#41 / #44), 新增字幕占位符; 支持 `[...]` 可选组; 支持 `{mosaic?}` / `{def?}` 等可空占位符; 支持 `{name|k=v}` 设置文本
- **NFO 标题前添加番号** (#43)
- **从 ThePornDB 刮削演员信息** (#51)
- **文件信息及水印** (#52), 支持马赛克/字幕/分辨率; 片库可按相关条件筛选; 整理时为图片添加水印; 前端展示水印
- **优化影片/演员详情页编辑面板**, 宽屏加宽, lightbox 预览, 设置页同款保存条; 演员头像可拖动排序

### 🐛 修复

- **桌面端默认关闭路径校验** (#49), 映射盘 / NAS / UNC 不再因白名单 403

## v0.5.0

### ✨ 新功能

- **媒体库文件过滤** (#13), 可设置正则规则跳过指定文件; 并在整理时将其移动到 `.amane_trash`, 可用于过滤广告视频等
- **增强演员别名** (#10)
- **路径模板新增 `{mosaic}`, `{definition}`, `{raw_name}`, `{raw_dir}`** (#4 / #24)
- **整理时自动移动字幕** (#9 / #23): 同目录字幕随视频一起整理

### 🐛 修复

- **macOS 15 App 无法启动** (#28)

## v0.4.2

### 🐛 修复

- **演员刮削** (#10): javdb 女优页默认性别、英文页兼容、搜索命中大小写不敏感; Wikidata 改多语言搜索 (ja/zh/en) + AV 关键词/职业判定. 伊藤舞雪、miru 可直接刮取并自动获得性别.
- **文件浏览器**: 挂载盘/跨盘路径兼容与规范路径化 (#8).
- **日志与网络盘**: LoggingMiddleware 置最外层收口请求日志; 网络盘失效不再无日志 500.
- **worker**: `stop()` 终止主循环, 消除在飞 claim 竞态.
- **Docker**: 镜像注入 `AMANE_LOG_DIR` (#5), compose 配置 `AMANE_SAFE_DIRS` (#6).

## v0.4.1

### 🐛 修复

- **文件浏览器跨盘符路径选择报 Internal Server Error** (#1): `os.path.commonpath` 在 Windows 不同盘符 (如 `C:` 与 `D:`) 上抛 `ValueError`, 导致 `/api/files` 浏览其他盘路径时返回 500. 现在跨盘路径视为非后代, 安全检查会跳过该盘并继续校验其余可信目录.
- **Windows 上资源 serve 404**: 资源库 `file_path` 之前按 OS 原生分隔符存储 (Windows 反斜杠), 与按 hash 前缀的查询模式不匹配; 统一存 POSIX (`/`) 分隔符.

### 🧪 测试 / CI

- **CI 测试门禁改为 Linux + Windows 双平台运行**: 此前只在 Ubuntu 上跑, Windows 专属用例 (如跨盘符路径测试) 全部被跳过, 这也是 #1 未被发现的原因.
- `export_openapi` 固定写 LF、新增 `.gitattributes` 统一文本行尾, 修复 Windows 上 `check-openapi` / `check-web` 因 CRLF 导致的整文件误报.
- 路径模板 / 二进制路径测试改为跨平台写法; `TestRepoRoundTrip` 改用独立文件库消除 FeedService 后台 poll 竞态.
